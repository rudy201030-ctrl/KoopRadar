(() => {
  "use strict";
  const $=s=>document.querySelector(s),grid=$("#dealGrid"),count=$("#resultCount"),empty=$("#empty"),input=$("#searchInput"),form=$("#searchForm"),sort=$("#sortSelect"),money=n=>Number.isFinite(Number(n))?new Intl.NumberFormat("nl-NL",{style:"currency",currency:"EUR"}).format(Number(n)):"—",esc=s=>String(s??"").replace(/[&<>\"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[m]));
  let products=[]; const base=String(window.KOOPRADER_API_URL||"").replace(/\/$/,"");
  const normalize=p=>({...p,id:p.id??p.ean??p.sku??p.slug,name:p.name||"Onbekend product",brand:p.brand||"",category:p.category||"",image_url:p.image_url||p.image||"",price:Number(p.lowest_total_price??p.price),oldPrice:Number(p.old_price??p.oldPrice),shops:Number(p.shop_count??p.shops??(p.offers?.length||0)),offers:Array.isArray(p.offers)?p.offers:[]});
  const sortProducts=(a,m)=>[...a].sort((x,y)=>m==="name"?String(x.name).localeCompare(String(y.name),"nl"):m==="saving"?((y.oldPrice-y.price)-(x.oldPrice-x.price)):m==="shops"?y.shops-x.shops:x.price-y.price);
  const card=p=>`<article class="deal-card"><div class="deal-visual">${p.image_url?`<img src="${esc(p.image_url)}" alt="${esc(p.name)}" loading="lazy">`:`<div class="deal-placeholder">${esc(p.brand||"KoopRader")}</div>`}</div><div class="deal-body"><span class="badge">Actuele feed</span><h3>${esc(p.name)}</h3><div class="shop">${esc(p.brand)}${p.category?` · ${esc(p.category)}`:""}${p.shops?` · ${esc(p.shops)} winkels`:""}</div><div class="prices"><strong>${money(p.price)}</strong>${p.oldPrice>p.price?`<del>${money(p.oldPrice)}</del>`:""}</div><div class="saving">${p.oldPrice>p.price?`Bespaar ${money(p.oldPrice-p.price)}`:"Actuele prijs"}</div><button class="deal-btn" data-id="${esc(p.id)}">Bekijk prijsvergelijking <span>→</span></button></div></article>`;
  function render(list){if(!grid)return;grid.innerHTML=list.map(card).join("");if(count)count.textContent=`${list.length} ${list.length===1?"resultaat":"resultaten"}`;if(empty)empty.hidden=list.length!==0;}
  async function get(path){const r=await fetch(base+path,{headers:{Accept:"application/json"},cache:"no-store"});if(!r.ok)throw Error(r.status);return r.json();}
  async function search(q="",category=""){grid.innerHTML='<div class="empty-state" style="grid-column:1/-1">Actuele producten laden…</div>';const p=new URLSearchParams({limit:"60"});if(q)p.set("q",q);if(category)p.set("category",category);if(sort?.value)p.set("sort",sort.value);for(const path of [`/api/search?${p}`,`/api/search.php?${p}`]){try{const d=await get(path),list=Array.isArray(d)?d:d.products;if(Array.isArray(list)){products=list.map(normalize).filter(x=>x.name&&Number.isFinite(x.price));render(sortProducts(products,sort?.value||"price"));return;}}catch(e){}}products=[];if(count)count.textContent="0 resultaten";if(empty)empty.hidden=true;grid.innerHTML='<div class="empty-state" style="grid-column:1/-1"><strong>Actuele productdata is tijdelijk niet beschikbaar.</strong><p>KoopRader toont bewust geen nepprijzen of demo-producten.</p></div>';}
  document.addEventListener("DOMContentLoaded",async()=>{const q=new URLSearchParams(location.search).get("q")||"",cat=new URLSearchParams(location.search).get("category")||"";if(input)input.value=q;await search(q,cat);form?.addEventListener("submit",async e=>{e.preventDefault();const q=input?.value.trim()||"";history.replaceState(null,"",q?`?q=${encodeURIComponent(q)}`:location.pathname);await search(q,"");$("#deals")?.scrollIntoView({behavior:"smooth"});});document.querySelectorAll("[data-search]").forEach(x=>x.addEventListener("click",async()=>{const q=x.dataset.search||"";if(input)input.value=q;history.replaceState(null,"",`?q=${encodeURIComponent(q)}`);await search(q,"");$("#deals")?.scrollIntoView({behavior:"smooth"});}));document.querySelectorAll("[data-category]").forEach(x=>x.addEventListener("click",async()=>{const c=x.dataset.category||"";if(input)input.value="";history.replaceState(null,"",`?category=${encodeURIComponent(c)}`);await search("",c);$("#deals")?.scrollIntoView({behavior:"smooth"});}));sort?.addEventListener("change",()=>render(sortProducts(products,sort.value)));document.addEventListener("click",async e=>{const b=e.target.closest(".deal-btn[data-id]");if(!b)return;const id=b.dataset.id;for(const path of [`/api/products/${encodeURIComponent(id)}`,`/api/product.php?id=${encodeURIComponent(id)}`,`/api/products.php?id=${encodeURIComponent(id)}`]){try{const p=normalize(await get(path)),offers=p.offers||[];const m=document.createElement("div");m.className="modal";m.innerHTML=`<div class="modal-card"><button class="close" aria-label="Sluiten">×</button><span class="badge">Actuele feed</span><h2>${esc(p.name)}</h2><p>${esc(p.brand)}${p.category?` · ${esc(p.category)}`:""}</p><div class="modal-price">Vanaf ${money(p.price)}</div><div class="modal-grid"><div><b>${esc(p.shops||0)}</b><small>winkels</small></div><div><b>${p.oldPrice>p.price?`${Math.round((1-p.price/p.oldPrice)*100)}%`:"—"}</b><small>besparing</small></div></div>${offers.length?`<h3>Actuele aanbiedingen</h3><div class="offers">${offers.map(o=>`<div class="offer"><div><b>${esc(o.shop_name||o.shop?.name||"Winkel")}</b><small>${o.total_price!=null?money(o.total_price):money(o.price)}</small></div><a class="deal-btn" href="${esc(o.product_url||o.url||"")}" target="_blank" rel="nofollow sponsored noopener">Naar winkel →</a></div>`).join("")}</div>`:`<p class="modal-note">Geen actuele aanbieding beschikbaar.</p>`}<button class="deal-btn close-action">Terug naar resultaten</button></div>`;document.body.appendChild(m);m.addEventListener("click",ev=>{if(ev.target===m||ev.target.closest(".close")||ev.target.closest(".close-action"))m.remove();});break;}catch(_){} }});});
})();