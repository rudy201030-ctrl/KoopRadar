const form=document.querySelector('#searchForm');const input=document.querySelector('#searchInput');const cards=[...document.querySelectorAll('.card')];const empty=document.querySelector('#empty');form.addEventListener('submit',e=>{e.preventDefault();const q=input.value.trim().toLowerCase();if(!q){cards.forEach(c=>c.hidden=false);empty.hidden=true;return}let found=0;cards.forEach(c=>{const ok=c.dataset.name.includes(q);c.hidden=!ok;if(ok)found++});empty.hidden=found>0;document.querySelector('#resultaten').scrollIntoView({behavior:'smooth'})});